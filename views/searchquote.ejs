<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>kereses</title>
</head>
<body>
    <form action="">
        <input type="text" name="input">
        <input type="submit">
    </form>

    <ul>
        <% searchResult.forEach(quote => { %>
            <li>
                <b><%= quote.author %></b>: "<%= quote.quote %>"
            </li>
        <% }) %>
    </ul>
</body>
</html>